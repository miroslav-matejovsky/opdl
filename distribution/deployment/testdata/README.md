# Deployment Test Data

This folder contains sample data fragments that demonstrate the deployment wire contract owned by the `deployment` package.

## Contents

- `descriptor.json`: A representative per-machine deployment descriptor formatted as JSON. It illustrates the JSON structure that the builder derives from topology and embeds into a target machine's platform binary, including machine identity, feature switches, and hosted services.
