# OPDL .NET SDK

A strongly-typed .NET client for the OPDL platform HTTP API.

The client under `src/Opdl.Sdk/Client/` is **generated**, not hand-written. It is
produced by [Kiota](https://learn.microsoft.com/openapi/kiota/) from the OpenAPI
contract at `contracts/openapi.yaml`, which is itself generated from the platform's
API description (`platform/api`). Both generation steps are owned by the
`conformance` module, so the SDK, the contract, and the code that serves it cannot
silently drift.

## Layout

```
sdk-dotnet/
  Opdl.Sdk.slnx                     solution
  src/Opdl.Sdk/
    Opdl.Sdk.csproj                 library project (references Microsoft.Kiota.Bundle)
    Client/                         Kiota output (generated; do not edit by hand)
      PlatformClient.cs             API client entry point
      Models/Status.cs             response models
      kiota-lock.json               generation manifest (contract hash, Kiota version)
```

## Regenerating the client

The client is regenerated by the conformance module, which drives Kiota. After the
contract changes, run:

```
go test ./conformance -run TestDotnetClient -update
```

On a normal test run (no `-update`) the same test regenerates into a temporary
directory and fails if the checked-in client is stale, so `task all` catches a
contract change that was not propagated here. That check needs Kiota installed and
the same version recorded in `kiota-lock.json`; it skips otherwise.

Install Kiota with:

```
dotnet tool install -g Microsoft.OpenApi.Kiota
```

## Building

```
dotnet build sdk-dotnet/Opdl.Sdk.slnx
```

or, from the repository root, `task sdk-dotnet`.

## Usage

The platform listens on a deployment-specific address, so the base URL is set on
the request adapter rather than baked into the contract. The status endpoint needs
no authentication.

```csharp
using Microsoft.Kiota.Abstractions.Authentication;
using Microsoft.Kiota.Http.HttpClientLibrary;
using Opdl.Sdk.Client;

var adapter = new HttpClientRequestAdapter(new AnonymousAuthenticationProvider())
{
    BaseUrl = "http://127.0.0.1:8080",
};

var client = new PlatformClient(adapter);

var status = await client.GetAsync();
Console.WriteLine($"{status?.StatusProp}: {status?.Message}");
// ok: platform is running
```

`StatusProp` carries the JSON `status` field; Kiota renames it to avoid clashing
with the generated `Status` model type.
